# PlatformIO build artifacts
.pio/
.vscode/

# Generated MSC disk image (rebuild via tools/build-msc-image.py)
firmware/src/msc_disk_image.h
firmware/src/msc_disk_image.bin

# Dead-end hybrid (framework = arduino, espidf) attempt — kept locally as
# postmortem material but not pushed. See feedback memory for lesson.
firmware-idf/

# IDF component manager auto-fetches these from the registry on first build.
# They're huge (M5GFX alone has 600+ MB of font/picture frame data) and should
# never be committed — dependencies.lock pins versions, registry does the rest.
firmware-idf-pure/managed_components/
firmware-idf-pure/dependencies.lock
firmware-idf-pure/sdkconfig.m5sticks3

# Generated MSC disk image (rebuild via tools/build-msc-image.py).
firmware-idf-pure/main/msc_disk_image.h
firmware-idf-pure/main/msc_disk_image.bin

# Local IDE / editor
.idea/
*.swp
*~

# macOS
.DS_Store

# Don't commit local config overrides
.env
.envrc
