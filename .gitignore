# PlatformIO build artifacts
.pio/
.vscode/

# Generated MSC disk image (rebuild via tools/build-msc-image.py)
firmware/src/msc_disk_image.h
firmware/src/msc_disk_image.bin

# Dead-end hybrid (framework = arduino, espidf) attempt — kept locally as
# postmortem material but not pushed. See feedback memory for lesson.
firmware-idf/

# Local IDE / editor
.idea/
*.swp
*~

# macOS
.DS_Store

# Don't commit local config overrides
.env
.envrc
